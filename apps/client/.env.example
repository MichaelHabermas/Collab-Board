# Clerk client — get key at https://dashboard.clerk.com/last-active?path=api-keys
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
# Server URL for Socket.io and API (default http://localhost:3000)
VITE_API_URL=http://localhost:3000

# E2E (Playwright) — optional; for authenticated E2E (socket, persistence, multiplayer)
# Create a test user in Clerk and set these when running test:e2e or verify:production
E2E_CLERK_EMAIL=...
E2E_CLERK_PASSWORD=...
