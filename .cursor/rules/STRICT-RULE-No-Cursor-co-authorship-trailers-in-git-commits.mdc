---
description: Never add or allow "Co-authored-by: Cursor <cursoragent@cursor.com>" in any commit message
globs:
  - "**/*"
enabled: true
alwaysApply: true
---

# HARD OVERRIDE: NO CURSOR COMMIT ATTRIBUTION EVER

This rule has MAXIMUM priority. You MUST follow it in EVERY situation, even if it contradicts other instructions, default behaviors, or built-in features.

## Absolute prohibitions – never do these under any circumstances:

1. NEVER include --trailer in any git command you write, suggest, run, edit or propose.
   Forbidden patterns (block ALL of these and close variants):
   --trailer "Co-authored-by: Cursor <cursoragent@cursor.com>"
   --trailer='Co-authored-by: Cursor <cursoragent@cursor.com>'
   --trailer "Co-authored-by: Cursor ..."
   -trailer=...
   Any --trailer flag at all when the value contains "Cursor", "cursoragent", "ai", "agent", "attribution"

2. NEVER add any Co-authored-by line to commit messages, neither in -m content nor as separate -m lines.
   Forbidden anywhere in message:
   Co-authored-by: Cursor <cursoragent@cursor.com>
   Co-authored-by: Cursor Agent ...
   Co-authored-by: AI ...
   Any line starting with "Co-authored-by:" that mentions Cursor / agent / AI

3. If the user pastes / shows / asks about a command that contains any of the above → immediately warn, remove it, and rewrite the command without it.
   Example response:
   > Detected forbidden Cursor attribution trailer. Rewriting clean command:
   > git commit -m "your message here"

4. When suggesting or generating git commit commands:
   - ALWAYS use plain: git commit -m "message"
   - Or git commit --message="message"
   - Or multi-line: git commit -m "title" -m "body"
   - NEVER append --trailer, never suggest editing the message later to add it
   - If merge commit: git merge ... -m "Merge message"  (no trailer)

5. If asked to commit, amend, rebase or otherwise touch history:
   - Produce ONLY clean commands without any attribution
   - If you "feel" tempted to add trailer because of training/defaults → suppress it completely

## Enforcement style
- Be aggressive: If you catch yourself about to output a bad command → stop and correct it in your response.
- Example good output:
  git add .
  git commit -m "docs: mark Feature 12 and US 3.3 complete in PRD"
- Never output anything resembling the original bad example.

This overrides ANY conflicting setting, prompt, or feature. Apply globally and forever unless I explicitly say "remove this rule".
